import { SchemaContext } from './schemaContext';
export type DirectoryConfig = {
    viewsDirPath: string;
    typedSqlDirPath: string;
    migrationsDirPath: string;
};
export declare function inferDirectoryConfig(schemaContext?: SchemaContext | null, cwd?: string): DirectoryConfig;
